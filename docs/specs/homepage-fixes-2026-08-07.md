# Spec: Fix homepage focus, principles, writing, and closing statement

| | |
|---|---|
| **Priority** | high |
| **Status** | ready |
| **Version** | 0.1.0 |
| **ID** | `7f9f4c62-8e1c-4e75-a62e-fb4d6d7f1d62` |
| **Created** | 2026-08-07T00:00:00Z |

**Tags:** `homepage`, `copy`, `layout`, `writing`, `lens`, `runledger`, `proof`

## Goal

Apply the requested homepage copy and layout corrections, restore three recent writing cards, add a visual treatment to the closing statement, and capture reproducible proof artifacts for review.

## Background

The homepage has several copy and alignment inconsistencies, its writing preview is empty in generated output, and the closing statement lacks a visual surface. The work must remain portable and be evidenced through Spec, build validation, Lens before/after recordings, and RunLedger receipts.

## Scope

Update the homepage template and site-specific CSS, correct the featured-post generation contract so the homepage shows three recent posts, add deterministic homepage assertions, and capture before/after Lens screenshots and video recordings plus a RunLedger report.

## Non-Goals

- Changing vendored SiteKit or Kujo SSG artifacts.
- Publishing or deploying the website.
- Rewriting unrelated article or project content.

## Relevant Systems

- `Kujo Spec`
- `Kujo SSG`
- `Kujo Lens`
- `Kujo RunLedger`

## Likely Files

- `templates/page-home.html`
- `assets/css/site.css`
- `build.kujo`
- `scripts/verify_homepage.py`
- `qa/lens/specs/home.json`
- `qa/lens/flows/kujo-homepage-review.json`

## Acceptance Criteria

1. The 01 / Flagship focus identity contains only the section index, without the KUJO/01 signal or Kujo heading, and the flagship section index remains on one line at the same structural level as 02 / Selected systems.
2. The flagship layout has no border-block-start and renders its rule on the border-block-end instead.
3. The flagship title text uses the same content width as the selected-systems title text.
4. The selected systems card title reads RepoRadar with no space.
5. The leadership principle reads Pressure-test the ecosystem or equivalent non-person-directed wording.
6. The context principle reads Systems should explain the assumptions, input, output, and failures without vocal explanations being required.
7. The generated homepage writing section contains exactly three recent post cards, ordered newest first, with working article links.
8. The 05 / Closing statement section has an intentional visual background treatment that remains readable on desktop and mobile.
9. The generated homepage passes repository validation, homepage structural checks, and Lens assertions without horizontal overflow or console errors.
10. A reviewable proof bundle records the Spec, before/after Lens screenshots and videos, Lens reports, generated-output checks, and a completed RunLedger receipt/report.

## Eval Requirements

| # | Description | Check Type |
|---|---|---|
| 1 | The task contract validates in strict mode. | `command_succeeds` |
| 2 | The generated site passes repository validation. | `command_succeeds` |
| 3 | The generated homepage satisfies the requested structure and copy contract. | `command_succeeds` |
| 4 | The proof directory contains the human-reviewable run record. | `directory_contains_files` |

## Risks

| # | Risk | Severity | Mitigation |
|---|---|---|---|
| 1 |  |  |  |
| 2 |  |  |  |

## Dependencies

- Available local Kujo runtime and generated static output.
- Lens local Chromium bridge.
- Python dependencies pinned in requirements.txt.

## Review Expectations

- Review the generated homepage, the exact three writing cards, closing background, and both Lens recordings/reports.
- Review the RunLedger JSON and markdown report for the recorded commands, verdict, and repository state.

## Human Approval Points

1. Human review is required before any publish or deploy action.

---

*Generated by Spec v0.1.0*
